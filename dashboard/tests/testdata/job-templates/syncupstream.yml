job:
  exception: raise
  execution: sequential
  name: upstream stats
  package: %PACKAGE_NAME%
  return_type: json
  tasks:
  - clone:
    - name: git repo
    - type: %REPO_TYPE%
    - branch: %REPO_BRANCH%
  - filter:
    - name: files
    - ext: PO
  - calculate: Stats
  type: syncupstream
