job:
  name: "sync downstream"
  buildsys: koji
  exception: raise
  package: dnf
  return_type: json
  tags:
    - rawhide
  tasks:
    - "GET latest builds"
    - "FETCH SRPM"
    - "Unpack SRPM"
    - "Load Spec File"
    - "Untar tarball"
    - "Filter PO files"
    - "Calculate Stats"
  type: i18nTestSRPM
